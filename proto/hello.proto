syntax = "proto3";

service Greeter {
	rpc Hello(HelloRequest) returns (HelloResponse) {}
}

message HelloRequest {
	string name = 1; // 1 is an index number, not a value assignment
}

message HelloResponse {
	string greeting = 2;
}

